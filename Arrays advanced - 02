function task(arr){
    let arr2 = []
    for(let i = 0; i < arr.length; i++){
        if(Number(arr[i]) < 0){
            arr2.unshift(Number(arr[i]))
        }else{
            arr2.push(Number(arr[i]))
        }
    }
    console.log(arr2.join('\n'))
}
